"""F010 川菜专家 — 菜系专属 prompt 片段 + 代表菜清单.

Per ADR-0003 「提示词 first-class 资产」:
- 提示词不是普通字符串, 而是与代码并列的资产, 必须独立维护、不与业务逻辑混在一起.
- 菜系片段优先用独立模块 / 字典 / 文本文件承载.
- 调用方通过 `from app.agents.cuisines.prompts.sichuan import ...` 之类的稳定 API.

## 内容来源

- `CUISINE_PROMPT_FRAGMENT` 对应 F010 spec §4, 填入 F003 §4 base prompt 的
  `{...菜系专属提示...}` 占位.
- `SIGNATURE_DISHES` 对应 F010 spec §3 代表菜清单.

与 spec 的差异说明详见 `app/agents/cuisines/sichuan.py` 模块 docstring.

## 调用方

- `app/agents/cuisines/sichuan.SichuanExpert` (F010 业务代码)
- `tests/unit/test_sichuan_expert.py` (测试断言菜品清单完整性)

模板字符串变更不应破坏调用方 — 业务流程只通过符号名引用常量.
"""

# F010 §3 — 代表性菜品清单 (与 `CUISINE_PROMPT_FRAGMENT` 内"代表菜"段同源).
# 分为四类 (经典 / 火锅 / 面食 / 小吃), 全部中文, 与 F003 §8.2 一致.
SIGNATURE_DISHES: tuple[str, ...] = (
    # 经典
    "麻婆豆腐",
    "回锅肉",
    "水煮鱼",
    "夫妻肺片",
    "口水鸡",
    # 火锅
    "麻辣火锅",
    "串串",
    "冒菜",
    # 面食
    "担担面",
    "宜宾燃面",
    "重庆小面",
    # 小吃
    "钵钵鸡",
    "兔头",
    "麻辣烫",
)

# F010 §4 — 菜系专属 prompt 片段, 填入 F003 §4 模板的 `{...菜系专属提示...}`.
#
# 提示词内嵌的菜品清单与上方 `SIGNATURE_DISHES` **必须保持一致** —
# 修改菜品时须同步两处; 后续若引入"单一来源 → 模板渲染"机制再消除重复.
CUISINE_PROMPT_FRAGMENT = """\
- 川菜特点: 麻辣鲜香, 重油重味, 擅用花椒与辣椒
- 代表菜（用于关键词生成, 至少选 2 个）:
  * 经典: 麻婆豆腐、回锅肉、水煮鱼、夫妻肺片、口水鸡
  * 火锅: 麻辣火锅、串串、冒菜
  * 面食: 担担面、宜宾燃面、重庆小面
  * 小吃: 钵钵鸡、兔头、麻辣烫
- 风味关键词: 麻辣、鲜香、糊辣、鱼香、花椒、火锅
- 与湘菜区别: 川偏"麻", 湘偏"香辣"; 用户只说"辣"时两者都可, 但说"麻辣"则优先川菜
- 与徽菜区别: 徽菜也重油但更"咸鲜"; 川菜偏"麻辣"
- 过敏原注意（重要）: 川菜常含花生油（麻婆豆腐）和花生碎（夫妻肺片、口水鸡）。
  如用户输入中出现"花生过敏"或 allergies 含 peanut,
  必须在 matched_allergies 中输出 "peanut", 并在 conclusion 中提示下单时确认
- 关键词生成: 必须包含 1 个菜系词（"川菜" / "四川" / "川味"）+
  2-3 个代表菜名 + 1-2 个风味词（"麻辣"、"花椒"、"火锅"）
- 餐厅筛选: 高德返回 ≥3 家时按"距离 + 评分"排序, 距离超 2km 降权"""
