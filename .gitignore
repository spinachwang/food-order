# ============ 环境与密钥 ============
.env
.env.local
.env.*.local
.env.old

# ============ Python ============
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
# 例外：F060 部署用的 env 模板目录不入 Python 虚拟环境忽略规则
!/deploy/env/

.conda/

# 测试 / 覆盖率
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# ============ Node / 前端 ============
node_modules/
dist/
build/
.vite/
*.local

# TypeScript 增量构建缓存（tsc -b 输出）+ tsconfig.node.json 引用的配置文件 emit 产物
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# ============ 日志与运行时 ============
*.log
logs/
.DS_Store
Thumbs.db

# ============ 调试产物（不入仓）============
# 高德 API 响应调试快照（由 scripts/amap_regeo_debug.py 产出）
backend/save_resp.json

# ============ 编辑器 ============
.vscode/
.idea/
*.swp
*.swo